# Artillery + Playwright Load Testing

This repository contains **load and performance testing scripts** using **Artillery** with the **Playwright engine**. The tests automate navigation through a sample web application and measure performance metrics like page load times, request counts, and errors.

---

## 🚀 Features

- Home page, Products, Cart, Login, Test Cases, API Testing, and Video Tutorials page navigation.
- Collects page load metrics and scenario execution statistics.
- Generates JSON reports compatible with **Artillery Cloud** for HTML visualization.
- Uses **TypeScript + Playwright** for scripting test flows.
- Supports custom timeouts and error handling for stable test execution.

---

## 📦 Installation

1. Clone the repository:

```bash
git clone https://github.com/JahidSDET/artillery-playwright.git
cd artillery-playwright
